# Conventions

Rules that apply to every workspace in Stack, regardless of which folder it's working in. The agent (Claude) reads this once per task, alongside the workspace's own `CONTEXT.md` — it does not get restated per-workspace.

## Comments

only on non-obvious WHY, never on WHAT.

## Code style

{Fill in: formatting, naming, language/framework defaults, anything you find yourself repeating across workspaces.}

## Writing style

- README files: always short and concise. 

## Anything else that's true everywhere

{Fill in as it comes up. One rule per bullet; link out to a longer file if a topic grows past a few lines.}
