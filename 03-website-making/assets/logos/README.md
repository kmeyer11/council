# logos

Logo files (svg/png) go here. Name variants explicitly — e.g. `logo-dark.svg`, `logo-icon.png` — don't rely on folder position to convey which is which. Record usage rules in `../brand.md`, not here.
