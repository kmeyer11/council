# Conventions

Rules that apply to every agent in orkester, regardless of which folder it's working in. An agent reads this once per task, alongside its own folder's `CONTEXT.md` — it does not get restated per-agent.

## Comments

only on non-obvious WHY, never on WHAT.

## Code style

{Fill in: formatting, naming, language/framework defaults, anything you find yourself repeating across agents.}

## Writing style

- README files: always short and concise. 

## Anything else that's true everywhere

{Fill in as it comes up. One rule per bullet; link out to a longer file if a topic grows past a few lines.}
